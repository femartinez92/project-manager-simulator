<% tasks = locals[:tasks] %>
<br>
<p> Aquí puedes seleccionar las tareas que quieras agregar al hito, debes agregar sólo las tareas que encuentres necesarias para el éxito del hito.</p>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Min (días) </th>
      <th>Moda (días) </th>
      <th>Max (días)</th>
      <th>Estimación Jefe Proyecto (días)</th>
      <th colspan="3">Agregar? </th>
    </tr>
  </thead>

  <tbody>
    <% tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td><%= task.min_duration %></td>
        <td><%= task.most_probable_duration %></td>
        <td><%= task.max_duration %></td>
        <td><%= task.pm_duration_estimation %></td>
        <td><%= check_box_tag "task_ids[]", task.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= submit_tag 'Agregar tareas', class: "button tiny success" %>
