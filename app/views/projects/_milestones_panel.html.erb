<div class="large-7 left"><h3> Hitos </h3></div>
    <div class="large-5 left"><%= link_to 'Ver hitos', project_milestones_path(@project), class: 'button tiny info ' %></div>
    <div class="large-12 left" id="milestones">
      <ul>
        <% @milestones.each do |milestone| %>
          <li> <%= link_to milestone.name.camelize, project_milestone_path(@project, milestone) %> </li>
        <% end -%>
      </ul>
    </div>